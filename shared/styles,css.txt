/* TradeAux Shared Design Tokens */
:root {
  --bg-primary: #020617;
  --bg-secondary: #0a1228;
  --bg-card: rgba(15, 32, 64, 0.85);
  --text-primary: #e5e7eb;
  --text-muted: #9ca3af;
  --accent: #7CFC00;
  --accent-secondary: #00ced1;
  --danger: #ff4d4d;
  --warning: #FFD966;
  --border-color: rgba(124, 252, 0, 0.3);
  --radius: 16px;
  --shadow: 0 10px 30px rgba(0,0,0,0.3);
  --font-display: 'Orbitron', system-ui, sans-serif;
  --font-body: 'Inter', system-ui, sans-serif;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  background: var(--bg-primary);
  color: var(--text-primary);
  font-family: var(--font-body);
  min-height: 100vh;
}

/* Typography */
h1, h2, h3, h4 { font-family: var(--font-display); }

/* Buttons */
.btn {
  padding: 8px 20px;
  border-radius: 10px;
  border: 1px solid rgba(0,206,209,0.3);
  background: var(--bg-secondary);
  color: var(--text-primary);
  cursor: pointer;
  transition: all 0.2s;
  font-family: var(--font-display);
  font-size: 13px;
  display: inline-flex;
  align-items: center;
  gap: 6px;
}
.btn:hover { border-color: var(--accent-secondary); transform: translateY(-1px); }
.btn-primary {
  background: linear-gradient(135deg, var(--accent-secondary), #00ff7f);
  color: #000;
  border: none;
  font-weight: 700;
}
.btn-primary:hover { box-shadow: 0 0 20px rgba(0,206,209,0.3); }
.btn-danger { border-color: rgba(255,77,77,0.4); color: var(--danger); }
.btn-danger:hover { background: rgba(255,77,77,0.15); }
.btn:disabled { opacity: 0.4; cursor: not-allowed; transform: none !important; }

/* Cards */
.card {
  background: linear-gradient(145deg, rgba(15,32,64,0.85), rgba(2,6,23,0.95));
  border: 1px solid rgba(0,206,209,0.3);
  border-radius: var(--radius);
  padding: 24px;
  margin-bottom: 20px;
  backdrop-filter: blur(8px);
}

/* Grids */
.grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
.grid-3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 16px; }
.grid-4 { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; }

/* Forms */
input, select, textarea {
  width: 100%;
  padding: 10px 14px;
  background: rgba(0,0,0,0.5);
  border: 1px solid rgba(0,206,209,0.25);
  border-radius: 10px;
  color: var(--text-primary);
  font-family: var(--font-body);
  font-size: 14px;
  transition: border 0.3s;
}
input:focus, select:focus, textarea:focus {
  outline: none;
  border-color: var(--accent-secondary);
  box-shadow: 0 0 0 3px rgba(0,206,209,0.2);
}

/* Toast */
#toastContainer {
  position: fixed;
  bottom: 24px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 99999;
  display: flex;
  flex-direction: column;
  gap: 8px;
  pointer-events: none;
}
.toast {
  padding: 12px 24px;
  border-radius: 40px;
  font-family: var(--font-display);
  font-size: 13px;
  background: var(--bg-secondary);
  border: 1px solid var(--border-color);
  color: var(--text-primary);
  opacity: 0;
  transform: translateY(20px);
  transition: all 0.3s;
  pointer-events: auto;
  max-width: 90vw;
  text-align: center;
}
.toast.show { opacity: 1; transform: translateY(0); }
.toast.success { border-color: var(--accent); color: var(--accent); }
.toast.error   { border-color: var(--danger); color: var(--danger); }
.toast.warning { border-color: var(--warning); color: var(--warning); }
.toast.info    { border-color: var(--accent-secondary); color: var(--accent-secondary); }

/* Spinner */
.spinner {
  width: 40px; height: 40px;
  border: 4px solid rgba(0,206,209,0.2);
  border-top-color: var(--accent-secondary);
  border-radius: 50%;
  animation: spin 1s linear infinite;
}
@keyframes spin { to { transform: rotate(360deg); } }

/* Modal */
.modal-overlay {
  display: none;
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.85);
  backdrop-filter: blur(12px);
  z-index: 10000;
  justify-content: center;
  align-items: center;
  padding: 20px;
}
.modal-overlay.active { display: flex; }
.modal-box {
  background: linear-gradient(145deg, #0a1228, #020617);
  border: 2px solid var(--accent-secondary);
  border-radius: 24px;
  max-width: 700px;
  width: 100%;
  max-height: 85vh;
  overflow-y: auto;
  padding: 28px;
  position: relative;
}
.modal-close {
  position: absolute;
  top: 16px; right: 20px;
  font-size: 24px; cursor: pointer;
  color: var(--text-muted); background: none; border: none;
}

/* Background Waves */
.bg-waves {
  position: fixed;
  inset: 0;
  z-index: 0;
  overflow: hidden;
  pointer-events: none;
}
.wave-bg {
  position: absolute;
  width: 200%;
  height: 200%;
  filter: blur(100px);
  opacity: 0.15;
  animation: waveBgDrift 24s ease-in-out infinite;
}
.wave-emerald-bg {
  background: radial-gradient(circle at 30% 40%, rgba(124,252,0,0.4), transparent 70%);
  top: -40%; left: -20%; animation-delay: 0s;
}
.wave-sapphire-bg {
  background: radial-gradient(circle at 50% 50%, rgba(0,206,209,0.4), transparent 70%);
  top: -30%; left: -10%; animation-delay: 8s;
}
.wave-ruby-bg {
  background: radial-gradient(circle at 70% 60%, rgba(158,74,74,0.4), transparent 70%);
  top: -20%; left: 0%; animation-delay: 16s;
}
@keyframes waveBgDrift {
  0% { transform: translate(0,0) scale(1); opacity: 0.15; }
  50% { transform: translate(40px,-30px) scale(1.05); opacity: 0.25; }
  100% { transform: translate(-30px,20px) scale(1); opacity: 0.15; }
}

/* Offline Badge */
.offline-badge {
  display: none;
  position: fixed;
  top: 70px;
  right: 20px;
  background: rgba(255,59,59,0.2);
  border: 1px solid var(--danger);
  color: var(--danger);
  padding: 4px 12px;
  border-radius: 20px;
  font-size: 11px;
  z-index: 9999;
  backdrop-filter: blur(4px);
}
.offline-badge.visible { display: block; }

/* Responsive */
@media (max-width: 768px) {
  .grid-2, .grid-3, .grid-4 { grid-template-columns: 1fr; }
}
@media (max-width: 1000px) and (min-width: 769px) {
  .grid-4 { grid-template-columns: 1fr 1fr; }
}